{
  "name": "nib",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "nib": "./dist/nib"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "bun run src/cli/index.ts",
    "build:cli": "bun build --compile src/cli/index.ts --outfile dist/nib",
    "build:lib": "bun build src/index.ts --outdir dist --target bun",
    "build": "bun run build:cli && bun run build:lib",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.0",
    "chalk": "^5.6.2",
    "chokidar": "^4.0.0",
    "citty": "^0.1.6",
    "ora": "^9.3.0",
    "smol-toml": "^1.3.1",
    "svg-pathdata": "^6.0.3",
    "svgson": "^5.3.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "5"
  }
}
